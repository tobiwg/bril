# Sum 1..n using a simple counted loop (no stdout).
# ARGS: 60000
@main(n: int) {
  s: int = const 0;
  i: int = const 1;
  one: int = const 1;

.loop:
  c = le i n;
  br c .body .done;

.body:
  s = add s i;
  i = add i one;
  jmp .loop;
.done:
print s;
ret;

}